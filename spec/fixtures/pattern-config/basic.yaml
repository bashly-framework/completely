patterns:
  - mygit init [init options] <directory>
  - mygit status|st [status options]

options:
  init:
    - --bare
  status:
    - --verbose|-v
    - --branch|-b <branch>

tokens:
  directory: directory
  branch: $(echo main dev)
